<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.CentralBank" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.CentralBank" parent="Base.Theme.CentralBank" />

    <style name="SCBSwitch" parent="Theme.AppCompat.Light">
        <!-- active thumb & track color (30% transparency) -->
        <item name="colorControlActivated">#2f8be4</item>

        <!-- inactive thumb color -->
        <item name="colorSwitchThumbNormal">#686868</item>

        <!-- inactive track color (30% transparency) -->
        <item name="android:colorForeground">#A6070707</item>
    </style>

    <style name="notif_button" parent="ThemeOverlay.Material3.Button">
        <!-- active thumb & track color (30% transparency) -->
        <item name="colorControlActivated">#2f8be4</item>

        <!-- inactive thumb color -->
        <item name="colorSwitchThumbNormal">#686868</item>

        <!-- inactive track color (30% transparency) -->
        <item name="android:colorForeground">#A6070707</item>
    </style>

    <style name="CustomAlertDialogStyle" parent="Theme.AppCompat.Light.Dialog.Alert">
        <item name="android:backgroundDimAmount">0.3</item>
    </style>

    <style name="Widget.App.RadioButton" parent="Widget.MaterialComponents.CompoundButton.RadioButton">
        <item name="buttonTint">@color/blue_string</item>
    </style>

</resources>